CREATE TABLE TopGames2 (
     id int PRIMARY KEY,
     GameName VARCHAR(100) NOT NULL,
     Rating DOUBLE PRECISION NOT NULL,
     Platform VARCHAR(20) NOT NULL,
     ReleaseDate VARCHAR(20) NOT NULL
);
